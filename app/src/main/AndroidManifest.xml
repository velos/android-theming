<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.velosmobile.velosandroidtheming">

    <application
        android:allowBackup="false"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/DefaultAppTheme"
        tools:ignore="GoogleAppIndexingWarning,MissingApplicationIcon">

        <activity
            android:launchMode="singleTop"
            android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:launchMode="singleTop"
            android:name=".activities.BulbasaurActivity"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/BulbasaurAppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <activity
            android:launchMode="singleTop"
            android:name=".activities.CharmanderActivity"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/CharmanderAppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

        <activity
            android:launchMode="singleTop"
            android:name=".activities.SquirtleActivity"
            android:parentActivityName=".activities.MainActivity"
            android:theme="@style/SquirtleAppTheme">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".activities.MainActivity" />
        </activity>

    </application>

</manifest>
